import logo from "../assets/logo.png";

const Header = () => {
    return (
        <header>
            <img src={logo} alt="logo" height="200px"></img>
        </header>
    );
};

export default Header;
